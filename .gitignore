# Build directories
#
# 带连字符的变体（build-ssl / build-webapp / build-nopool / build-dbg / build-test）
# 原先**一条规则都不匹配**。它们之所以大多没有出现在 `git status` 里，是因为
# CMake 构建树的内容恰好被下面的 `*.txt` / `*.cmake` / `CMakeFiles/` / `_deps/`
# 兜住了 —— 那是**巧合，不是覆盖**：只要树里出现一个不匹配那些模式的产物
# （.log / .dll / .exe / .obj / .json …），整个目录就会变成未跟踪，
# 一次 `git add -A` 就把它整个吞进去。
#
# 用带尾斜杠的形式（只匹配目录）：仓库里有一个**已被跟踪**的文件
# `build-ssl-run.sh`，写成 `build*` 会连它一起匹配上。
build/
build_*/
build-*/
build_noexpand/
build_nopool/
out/
_deps/
_local_*/
Testing/

# 测试临时目录（用例按相对名在当前目录建）
#
# 上传用例按**相对名**建目录（web_app_upload_func.cpp:1838 的
# uvcpp_upload_test_dir、web_app_upload_route_func.cpp:2084 的
# uvcpp_rt_outside_uploads），每跑一次就出现一次，里面留着上一轮没被清掉的
# 残留部件。**不写成带前导斜杠的形式** —— 用例的工作目录可能是仓库根，
# 也可能是构建树（ctest 从哪跑取决于调用方式），锚定死一处就漏另一处。
uvcpp_upload_test_dir/
uvcpp_rt_outside_uploads/

# Development docs / temp
docs/
*.txt
# tests/tools/*.py 之间有 import（upload_mutation_probe / v5_probe 会被主驱动拉起），
# 跑一次就在 tests/tools/ 下留一个 __pycache__/。它同样是未跟踪状态，
# 步骤 9 那轮是靠手工删掉的。
__pycache__/
*.pyc
websocket_parser.h
websocket_parser.c
stdout.txt
stderr.txt

# IDE
.vs/
.vscode/
*.user
*.suo

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
*.cmake
!cmake/uvcppConfig.cmake.in

# OS
.DS_Store
Thumbs.db

# Package
*.tar.gz
*.zip
*.7z
